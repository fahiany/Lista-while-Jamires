#include <stdio.h>
#include <stdlib.h>

int main() {
    int numero;

  
    printf("Digite um número para iniciar a contagem regressiva: ");
    scanf("%d", &num);

    while (numero >= 0) {
        printf("%d\n", num);
        num--; 
    }

    system("pause");
    return 0;
}
